function forloopexamplee() {
 for (let x = 1; x <= 10; x++){       //(For loop syntax (Variable; Condition; Increment or decrement))
    let mul = 5 * x;                 // For specifying Mul here
    let u = "5 *"+ x + "=" + mul     //For printing purpose only 
console.log(u);
}
}